import Container from '../Container/index';

const Messages = ({ children }) => {
    return (
        <Container>
            <h1>Messages View</h1>
        </Container>
    );
};

export default Messages;
