import Container from '../Container/index';
import Subheading from '../Subheading/index';

const SearchResults = () => {
    return (
        <Container>
            <Subheading underlined>Results for</Subheading>
        </Container>
    );
};

export default SearchResults;
