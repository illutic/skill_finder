/** @constant MESSAGE_TYPES */
/** Specifies the type of the message that a user can send via chat.
 */
const MESSAGE_TYPES = {
    text: 'text',
    code: 'code',
    file: 'file',
    image: 'image',
};

export default MESSAGE_TYPES;
